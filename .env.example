# Ingestion (Python) — service-role key, never exposed to the browser
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_KEY=<service-role-key>

# Optional HTTP tuning for the fetchers
HTTP_TIMEOUT=30
# HTTP_USER_AGENT=
